#!/usr/bin/env node

'use strict'

process.title = 'static-https-server'

const path = require('path')
const fs = require('fs')
const lib = path.join(path.dirname(fs.realpathSync(__filename)), '../')

require(lib + 'server.js')
